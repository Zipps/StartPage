import React from 'react';

import classes from './BookmarkForm.module.css';
import { Bookmark } from '../../../store/Bookmarks';

const bookmarkFormComponent = (props: Bookmark) => {

    return <form>
            </form>
}
export default bookmarkFormComponent;
